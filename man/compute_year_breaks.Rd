% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{compute_year_breaks}
\alias{compute_year_breaks}
\title{Compute year break positions for timeline annotation}
\usage{
compute_year_breaks(
  from,
  to,
  breaks = "auto",
  labels = NULL,
  side = c("alternate", "above", "below", "inside"),
  axis_y = 0,
  colours = NULL,
  start = NULL,
  end = NULL
)
}
\arguments{
\item{from, start}{Start date (Date, POSIXct, or numeric). \code{start} is an alias.}

\item{to, end}{End date. \code{end} is an alias.}

\item{breaks}{Year interval specification:
\itemize{
\item \code{"auto"} picks a sensible interval from the date span.
\item A character string like \code{"1 year"}, \code{"2 years"}, \code{"5 years"}.
\item A numeric vector of years (converted to mid-year dates).
\item A Date vector of explicit break positions.
}}

\item{labels}{Character labels; defaults to formatted years.}

\item{side}{Placement relative to the axis: \code{"alternate"} (default),
\code{"above"}, \code{"below"}, or \code{"inside"} (centered in a thick bar arrow).}

\item{axis_y}{Y position of the timeline axis.}

\item{colours}{Optional character vector of colours cycling across years.}
}
\value{
A data frame with columns \code{x}, \code{label}, \code{y}, \code{.timeline_year_side},
and optionally \code{colour}.
}
\description{
Generates a data frame of year labels suitable for \code{\link[=geom_timeline_year]{geom_timeline_year()}}.
}
\examples{
compute_year_breaks(
  from = as.Date("2020-01-01"),
  to = as.Date("2026-12-01"),
  breaks = "2 years"
)
}
