% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggtimeline-swimlane.R
\name{facet_timeline}
\alias{facet_timeline}
\title{Facet helper for swimlane-style timelines}
\usage{
facet_timeline(facets, ncol = 1, scales = "free_y", ...)
}
\arguments{
\item{facets}{A facetting specification (formula or \code{\link[ggplot2:vars]{ggplot2::vars()}}).}

\item{ncol}{Number of columns. Defaults to \code{1} (one row per facet).}

\item{scales}{Axis scale sharing across panels; see
\code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}. Defaults to \code{"free_y"} so each panel keeps its
own label stacking range.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}}.}
}
\value{
A ggplot2 \code{Facet} object, addable via \code{+} to a \code{ggtimeline()} plot.
}
\description{
Thin wrapper over \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}} intended for use with
\code{\link[=ggtimeline]{ggtimeline()}} output when a per-panel (rather than stacked-lane) layout
is preferred. See \code{\link[=ggtimeline_swimlane]{ggtimeline_swimlane()}} for a single-panel, stacked
alternative.
}
\examples{
\donttest{
library(ggplot2)
data("phenotype_methods_timeline")
ggtimeline(
  phenotype_methods_timeline,
  aes(x = date, label = topic, fill = category)
) +
  facet_timeline(~category)
}
}
\seealso{
\code{\link[=ggtimeline_swimlane]{ggtimeline_swimlane()}}
}
