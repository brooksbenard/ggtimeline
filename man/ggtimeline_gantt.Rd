% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggtimeline-gantt.R
\name{ggtimeline_gantt}
\alias{ggtimeline_gantt}
\title{Gantt-chart timeline variant}
\usage{
ggtimeline_gantt(
  data,
  mapping,
  bar_height = 0.55,
  row_spacing = 1,
  label_position = c("inside", "axis", "none"),
  label_size = 3.2,
  label_colour = NULL,
  date_breaks = ggplot2::waiver(),
  date_labels = "\%Y",
  background = "white",
  ...
)
}
\arguments{
\item{data}{Event / interval data frame.}

\item{mapping}{An \code{\link[ggplot2:aes]{ggplot2::aes()}} mapping. Requires \code{x} (start date) and
\code{label} (row/bar text). Optional \code{xend} (or \code{xmax}) for interval bars;
optional \code{y} to control row grouping/order explicitly (otherwise rows
are one per unique \code{label}, in first-appearance order). Other optional
aesthetics include \code{fill} and \code{colour}.}

\item{bar_height}{Vertical thickness of each Gantt bar (in row units,
\code{0}-\code{1}).}

\item{row_spacing}{Vertical distance between row centres.}

\item{label_position}{Where row labels are drawn: \code{"inside"} (default,
left-aligned inside/near the bar start), \code{"axis"} (as y-axis row
labels only), or \code{"none"} (no text labels; still shows the y axis
categories).}

\item{label_size}{Text size for in-plot row labels.}

\item{label_colour}{Colour for in-plot row labels. Defaults to a colour
that contrasts with \code{background}.}

\item{date_breaks, date_labels}{Passed to \code{\link[ggplot2:scale_date]{ggplot2::scale_x_date()}}.}

\item{background}{Plot background colour.}

\item{...}{Reserved for forward compatibility.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Same aesthetic grammar as \code{\link[=ggtimeline]{ggtimeline()}} but renders one horizontal bar
per row (grouped by an explicit \code{y} aesthetic, or by \code{label} when \code{y} is
not mapped) instead of a single shared arrow axis. Point events (no
\code{xend}/\code{xmax}) are drawn as short bars so they stay visible.
}
\examples{
\donttest{
library(ggplot2)
trials <- data.frame(
  start = as.Date(c("2021-01-01", "2022-06-01", "2023-01-01")),
  end   = as.Date(c("2021-09-01", "2023-02-01", "2023-08-01")),
  topic = c("Phase I", "Phase II", "Phase III"),
  arm   = c("A", "B", "A")
)
ggtimeline_gantt(trials, aes(x = start, xend = end, label = topic, fill = arm))
}
}
