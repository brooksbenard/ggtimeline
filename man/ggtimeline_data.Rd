% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate-data.R
\name{ggtimeline_data}
\alias{ggtimeline_data}
\title{Validate timeline input data}
\usage{
ggtimeline_data(
  data,
  date = "date",
  start = NULL,
  end = NULL,
  label = "topic",
  require_label = TRUE
)
}
\arguments{
\item{data}{A data frame of timeline events.}

\item{date}{Name of the event date column for point events. Ignored when
both \code{start} and \code{end} are supplied.}

\item{start, end}{Optional start/end date columns for interval events
(mapped to \code{x} / \code{xend} in \code{\link[=ggtimeline]{ggtimeline()}}).}

\item{label}{Name of the label column.}

\item{require_label}{If \code{TRUE}, require \code{label} to exist.}
}
\value{
The input \code{data}, invisibly (after light checks).
}
\description{
Checks that a data frame has the columns needed by \code{\link[=ggtimeline]{ggtimeline()}} and
returns it (invisibly) after light validation. Use before plotting to get
clearer errors than a cryptic ggplot failure.
}
\examples{
data("phenotype_methods_timeline")
ggtimeline_data(phenotype_methods_timeline, date = "date", label = "topic")

intervals <- data.frame(
  start = as.Date(c("2021-01-01", "2022-01-01")),
  end = as.Date(c("2021-06-01", "2022-09-01")),
  topic = c("A", "B")
)
ggtimeline_data(intervals, start = "start", end = "end", label = "topic")
}
