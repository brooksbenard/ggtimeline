% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggtimeline-swimlane.R
\name{ggtimeline_swimlane}
\alias{ggtimeline_swimlane}
\title{Swimlane timeline with parallel category arrows}
\usage{
ggtimeline_swimlane(
  data,
  mapping,
  lane_spacing = 3.2,
  side = c("auto", "alternate", "above", "below"),
  elbowed = FALSE,
  base_height = 1.1,
  height_step = 0.75,
  label_width_days = 100,
  min_gap_days = 21,
  label_method = "simple",
  label_size = 3,
  axis_height = 0.42,
  axis_tip = 0.015,
  axis_fill = "white",
  connector_colour = "#A8A8A4",
  connector_size = 0.45,
  label_box = TRUE,
  span_height = 0.12,
  span_alpha = 0.8,
  eras = NULL,
  era_alpha = 0.16,
  year_breaks = NULL,
  year_labels = NULL,
  year_size = 4.8,
  year_colours = NULL,
  expand = 0.1,
  lane_labels = TRUE,
  lane_label_size = 3.6,
  background = "white",
  ...
)
}
\arguments{
\item{data}{Event data frame.}

\item{mapping}{Aesthetics; must include \code{x} (event date) and \code{label}
(topic text), plus a grouping column via \code{group}, \code{fill}, or \code{colour}
(checked in that order) to determine lanes. Optional \code{xend}/\code{xmax} for
interval events (drawn as span bars), and other aesthetics as in
\code{\link[=ggtimeline]{ggtimeline()}}.}

\item{lane_spacing}{Vertical distance between consecutive lane axes.}

\item{side}{Label placement per lane: \code{"auto"} (default), \code{"alternate"},
\code{"above"}, or \code{"below"}.}

\item{elbowed}{If \code{TRUE}, use elbowed connectors.}

\item{base_height, height_step, label_width_days, min_gap_days, label_method}{Layout parameters; see \code{\link[=ggtimeline]{ggtimeline()}}.}

\item{label_size}{Topic label text size.}

\item{axis_height, axis_tip, axis_fill}{Bar axis appearance; see
\code{\link[=geom_timeline_axis]{geom_timeline_axis()}}.}

\item{connector_colour, connector_size}{Connector line appearance.}

\item{label_box}{If \code{TRUE} (default), draw boxed labels.}

\item{span_height, span_alpha}{Interval span-bar appearance (used only
when \code{xend}/\code{xmax} is mapped).}

\item{eras}{Optional era bands spanning the full lane stack; see
\code{\link[=geom_timeline_era]{geom_timeline_era()}} / \code{\link[=ggtimeline]{ggtimeline()}}.}

\item{era_alpha}{Default era band opacity.}

\item{year_breaks, year_labels, year_size, year_colours}{Year annotations
drawn once above the top lane; see \code{\link[=compute_year_breaks]{compute_year_breaks()}}.}

\item{expand}{Fraction of the event date span used for horizontal padding.}

\item{lane_labels}{If \code{TRUE} (default), draw the lane/group name to the
left of each lane's axis.}

\item{lane_label_size}{Text size for lane name labels.}

\item{background}{Plot background colour.}

\item{...}{Reserved for forward compatibility.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Stacks one ribbon-style arrow lane per group (e.g. method family or
project stream) on a single shared date axis. Each lane gets its own
thick bar axis, connectors, and labels; era bands and year breaks (when
requested) span the full stack.
}
\examples{
\donttest{
library(ggplot2)
data("phenotype_methods_timeline")
ggtimeline_swimlane(
  phenotype_methods_timeline,
  aes(x = date, label = topic, group = category, fill = category),
  lane_spacing = 3
)
}
}
\seealso{
\code{\link[=facet_timeline]{facet_timeline()}}, \code{\link[=ggtimeline]{ggtimeline()}}
}
