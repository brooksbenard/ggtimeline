% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggtimeline-package.R, R/ggtimeline.R
\docType{package}
\name{ggtimeline}
\alias{ggtimeline-package}
\alias{ggtimeline}
\title{ggtimeline: Flexible timeline visualisations for ggplot2}
\usage{
ggtimeline(
  data,
  mapping,
  style = c("classic", "ribbon", "minimal", "milestone"),
  side = c("auto", "alternate", "above", "below"),
  elbowed = TRUE,
  base_height = 0.8,
  height_step = 0.55,
  label_width_days = 90,
  min_gap_days = 14,
  axis_y = 0,
  date_breaks = ggplot2::waiver(),
  date_labels = "\%Y",
  expand = 0.06
)
}
\arguments{
\item{data}{A data frame containing timeline events.}

\item{mapping}{An \code{\link[ggplot2:aes]{ggplot2::aes()}} mapping. Required aesthetics are
\code{x} (date) and \code{label} (topic text). Optional aesthetics include
\code{colour}, \code{fill}, \code{shape}, \code{size}, \code{linetype}, \code{alpha}, and \code{group}
for shared styling.}

\item{style}{Timeline visual style: \code{"classic"} (axis points with ring
endpoints), \code{"ribbon"} (thick bar with boxed labels), \code{"milestone"}
(boxed labels with endpoint markers), or \code{"minimal"} (compact dotted
connectors).}

\item{side}{Label placement: \code{"auto"} (default), \code{"alternate"}, \code{"above"},
or \code{"below"}.}

\item{elbowed}{If \code{TRUE} (default), use elbowed connectors.}

\item{base_height}{Base distance from axis to first label tier.}

\item{height_step}{Additional vertical offset per overlap tier.}

\item{label_width_days}{Approximate label width for overlap detection.}

\item{min_gap_days}{Minimum horizontal gap between labels on the same side.}

\item{axis_y}{Y position of the timeline axis.}

\item{date_breaks}{Date breaks passed to \code{\link[ggplot2:scale_date]{ggplot2::scale_x_date()}}.}

\item{date_labels}{Date label format passed to \code{\link[ggplot2:scale_date]{ggplot2::scale_x_date()}}.}

\item{expand}{Axis padding beyond first/last events (fraction of range).}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object that can be further customised with
standard ggplot2 layers, scales, and themes.
}
\description{
Create publication-ready timeline charts with multiple visual styles,
elbowed connectors, and automatic label placement.

High-level interface for building timeline visualisations with sensible
defaults. Accepts a data frame with date, topic, and optional aesthetic
columns. Automatically places labels above and below the axis, staggers
heights to reduce overlap, and draws elbowed connectors.
}
\examples{
\donttest{
library(ggplot2)
data("phenotype_methods_timeline")

ggtimeline(
  phenotype_methods_timeline,
  aes(x = date, label = topic, colour = category, fill = category),
  style = "classic"
) +
  scale_timeline_colour() +
  scale_timeline_fill()
}
}
\seealso{
Useful links:
\itemize{
  \item \url{https://github.com/brooksbenard/ggtimeline}
  \item Report bugs at \url{https://github.com/brooksbenard/ggtimeline/issues}
}

}
\author{
Brooks Benard (\email{brooksbenard10@gmail.com})
}
\keyword{internal}
