% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggtimeline.R
\name{ggtimeline}
\alias{ggtimeline}
\title{Create a timeline plot}
\usage{
ggtimeline(
  data,
  mapping,
  style = "ribbon",
  side = c("auto", "alternate", "above", "below"),
  elbowed = NULL,
  base_height = 1.1,
  height_step = 0.75,
  label_width_days = 100,
  label_size = 3,
  min_gap_days = 21,
  label_method = NULL,
  axis_y = 0,
  axis_shape = NULL,
  axis_height = NULL,
  axis_width = NULL,
  axis_tip = NULL,
  axis_fill = NULL,
  axis_arrow = TRUE,
  start_cap = NULL,
  connector_colour = NULL,
  connector_size = 0.45,
  show_points = NULL,
  eras = NULL,
  era_alpha = 0.16,
  era_label_size = NULL,
  era_label_colour = NULL,
  year_breaks = NULL,
  year_labels = NULL,
  year_side = NULL,
  year_size = 4.8,
  year_colours = NULL,
  year_offset = 0.32,
  year_lines = FALSE,
  year_line_colour = NULL,
  year_line_size = 0.35,
  year_line_width = NULL,
  year_line_alpha = 0.85,
  year_line_linetype = "dashed",
  date_breaks = ggplot2::waiver(),
  date_labels = "\%Y",
  expand = 0.1,
  background = "white"
)
}
\arguments{
\item{data}{A data frame containing timeline events.}

\item{mapping}{An \code{\link[ggplot2:aes]{ggplot2::aes()}} mapping. Required aesthetics are
\code{x} (date) and \code{label} (topic text). Optional aesthetics include
\code{colour}, \code{fill}, \code{shape}, \code{size}, \code{linetype}, \code{alpha}, and \code{group}
for shared styling.}

\item{style}{Visualisation type. Currently only \code{"ribbon"} is available.}

\item{side}{Label placement: \code{"auto"} (default), \code{"alternate"}, \code{"above"},
or \code{"below"}.}

\item{elbowed}{If \code{TRUE}, use elbowed connectors. Defaults to \code{FALSE}
(straight vertical stems).}

\item{base_height}{Base distance from the axis to the first label tier.}

\item{height_step}{Additional vertical offset per overlap tier on the same
side. Increase to add more space between stacked labels.}

\item{label_width_days}{Approximate horizontal label width for overlap
detection.}

\item{label_size}{Topic label text size (mm, ggplot2 text size). Applied
directly to plain and boxed labels.}

\item{min_gap_days}{Minimum horizontal gap between labels on the same side.}

\item{label_method}{Label placement algorithm: \code{"simple"} (default) stacks
overlapping labels vertically with straight connectors; \code{"auto"} /
\code{"mark"} allow horizontal spreading; \code{"repel"} adds a repulsion pass
(soft dependency on \strong{ggrepel}).}

\item{axis_y}{Y position of the timeline axis.}

\item{axis_shape}{Axis geometry: \code{"bar"} (thick arrow body with a normal tip;
years fit inside) or \code{"line"}. Defaults by style.}

\item{axis_height, axis_width}{Half-height (vertical thickness) of the bar
arrow in y-units. \code{axis_width} is an alias for \code{axis_height}; if both are
set, \code{axis_width} wins.}

\item{axis_tip}{Fraction of the axis length used for the right arrowhead
depth. Smaller values give a shorter, less stretched tip. Defaults to
\code{0.015}.}

\item{axis_fill}{Interior fill colour of the bar arrow.}

\item{axis_arrow}{If \code{TRUE} (default), draw a closed arrowhead at the
right end when \code{axis_shape = "line"}. Bar tips are always drawn.}

\item{start_cap}{If \code{TRUE}, draw a filled dot at the left origin of a line
axis. Ignored for bar axes.}

\item{connector_colour}{Colour for connector lines. Defaults to a
style-specific colour.}

\item{connector_size}{Width of connector lines (ggplot2 line size).}

\item{show_points}{If \code{TRUE}, draw event markers where connectors meet the
axis. Defaults to \code{FALSE} (clean stems). When a \code{shape} aesthetic is
mapped and points are shown, markers use that scale and sit on the bar
edge (not the ribbon centre).}

\item{eras}{Optional data frame of background era bands with \code{start}/\code{end}
(or \code{xmin}/\code{xmax}) and optional \code{label}, \code{fill}/\code{colour}, and \code{alpha}
columns. See \code{\link[=geom_timeline_era]{geom_timeline_era()}}.}

\item{era_alpha}{Default opacity for era bands when \code{eras$alpha} is missing.}

\item{era_label_size}{Text size for era labels drawn at the top of each band.}

\item{era_label_colour}{Colour for era labels. Defaults to a darkened band
colour when \code{NULL}.}

\item{year_breaks}{Year annotations along the axis. \code{NULL} omits year labels.
\code{"auto"} picks a sensible interval from the date span. A string like
\code{"2 years"} or \code{"5 years"} sets a fixed interval. A numeric vector of
years or Date vector sets explicit positions. See \code{\link[=compute_year_breaks]{compute_year_breaks()}}.}

\item{year_labels}{Optional character labels for year breaks.}

\item{year_side}{Placement of year labels: \code{"inside"} (default for bar
styles; years sit in the arrow body), \code{"alternate"}, \code{"above"}, or
\code{"below"}.}

\item{year_size}{Year label text size.}

\item{year_colours}{Optional character vector of colours for year labels.
Defaults to the axis colour when \code{year_side = "inside"}.}

\item{year_offset}{Distance of year labels from the axis in y-units.
Ignored when \code{year_side = "inside"}.}

\item{year_lines}{Optional dashed vertical year-boundary ticks drawn
\strong{inside the timeline arrow} only (not full-panel era bounds).
\code{FALSE} (default) omits them. \code{TRUE} draws a tick every year (or matches
the numeric interval in \code{year_breaks} when that is like \code{"2 years"}).
An integer (or string like \code{"2 years"}) sets the years between ticks.}

\item{year_line_colour}{Colour for in-arrow year ticks. Defaults to a
medium grey.}

\item{year_line_size, year_line_width}{Width of in-arrow year ticks
(\code{year_line_width} is an alias for \code{year_line_size}).}

\item{year_line_alpha}{Opacity of in-arrow year ticks (0–1). Defaults to
\code{0.85}.}

\item{year_line_linetype}{Linetype for in-arrow year ticks (default
\code{"dashed"}).}

\item{date_breaks}{Date breaks for the (hidden) x scale; used mainly when
\code{year_breaks = NULL}.}

\item{date_labels}{Date label format for the x scale when \code{year_breaks = NULL}.}

\item{expand}{Fraction of the event date span used mainly for overall scale
and right-side tip clearance. The left arrow edge stays close to the first
event (a small fraction of this pad); the right side still grows enough to
clear labels and the arrow tip.}

\item{background}{Plot background colour.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object that can be further customised with
standard ggplot2 layers, scales, and themes.
}
\description{
High-level interface for building timeline visualisations with sensible
defaults. Accepts a data frame with date, topic, and optional aesthetic
columns. The current visualisation is a \strong{ribbon} timeline: thick bar
arrow with boxed labels, vertical connectors that stack when labels would
overlap, optional in-arrow years/era bands, and optional edge markers.
Additional visualisation types may be added later via \code{style}.
}
\examples{
\donttest{
library(ggplot2)
data("phenotype_methods_timeline")

eras <- data.frame(
  start = as.Date(c("2020-07-01", "2023-01-01", "2025-01-01")),
  end = as.Date(c("2022-12-31", "2024-12-31", "2026-12-31")),
  label = c("Emergence", "Expansion", "Acceleration"),
  fill = c("#4C72B0", "#55A868", "#C44E52")
)

ggtimeline(
  phenotype_methods_timeline,
  aes(x = date, label = topic, fill = category, shape = status),
  style = "ribbon",
  year_breaks = "1 year",
  eras = eras,
  base_height = 1.3,
  height_step = 0.9
) +
  scale_timeline_fill() +
  scale_timeline_shape()
}
}
