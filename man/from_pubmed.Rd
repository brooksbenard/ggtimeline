% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import-bib.R
\name{from_pubmed}
\alias{from_pubmed}
\title{Import timeline rows from PubMed}
\usage{
from_pubmed(
  ids,
  api_key = getOption("ggtimeline.ncbi_key"),
  quiet = FALSE,
  ...
)
}
\arguments{
\item{ids}{Character or numeric vector of PMIDs.}

\item{api_key}{Optional NCBI API key (raises rate limits). Defaults to
\code{getOption("ggtimeline.ncbi_key")}.}

\item{quiet}{If \code{FALSE} (default), warn about IDs that failed to resolve.}

\item{...}{Reserved for forward compatibility.}
}
\value{
A data frame with columns \code{date}, \code{topic} (title), \code{pmid}, and
\code{citations} (when available from the summary payload; \code{NA} otherwise).
}
\description{
Resolves PubMed IDs (PMIDs) into a \code{\link[=ggtimeline]{ggtimeline()}}-ready data frame via
the NCBI E-utilities \code{esummary} endpoint. Requires the \strong{httr2}
(preferred) or \strong{httr} package, plus \strong{jsonlite}.
}
\examples{
\dontrun{
from_pubmed(c("32015508", "31978945"))
}
}
